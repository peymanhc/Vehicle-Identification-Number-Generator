
interface VehicleInfoTypes {
  version: string;
  equipmentCode: string;
  year: string;
  place: string;
  serialNumber: string;
  vin: string;
  searchSerialNumber: string;
  vinList: Array<string>;
}
